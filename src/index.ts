/**
 * Presumably, your library exports a `thing` the does _something_.
 *
 * This `thing` is a function that when called returns the number `42`.
 *
 * @returns The number `42`.
 */
export function thing(): number {
  return 42;
}
